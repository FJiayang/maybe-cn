 <%= render DS::Button.new(
        text: t("common.actions.sign_out"),
        icon: "log-out",
        icon_position: :right,
        variant: "ghost",
        href: session_path(Current.session),
        method: :delete
      ) %>
